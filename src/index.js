import React from 'react'
import ReactDOM from 'react-dom'
import './index.css';

ReactDOM.render(
  React.createElement('div', null, `Complete Guid Webpack Configuration for React`),
  document.getElementById('app')
)